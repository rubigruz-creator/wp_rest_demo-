name: wp_rest_demo
description: "WP REST API Demo Application"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.0.4
  # file_picker: ^5.5.0  # ВРЕМЕННО КОММЕНТИРУЕМ
  url_launcher: ^6.1.11
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true